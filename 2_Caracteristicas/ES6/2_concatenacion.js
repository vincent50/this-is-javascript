// Template literal
let hello = 'Hello'
let world = 'World'

// ES5
let epicPhrase = hello + ' ' + world
console.log(epicPhrase)

// ES6
let epicPhrase2 = `${hello} ${world}` // con comillas francesas
console.log(epicPhrase2)
